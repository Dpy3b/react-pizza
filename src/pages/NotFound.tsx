import { FC } from 'react';
import NotFoundBlock from '../components/NotFoundBlock/NotFoundBlock';

const NotFound:FC = () => {
	return (
		<NotFoundBlock /> // типа можно обернуть в родителя и ещё чето сюда запихнуть при желании
	);
};

export default NotFound;
